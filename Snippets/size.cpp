#include <vector>
#include <string>
#include <iostream>
#define sz(a) ((int)(a).size())

using namespace std;

int main(){
    string t = "Hello, what's up";
    vector<int> c (10);
    cout << sz(t) << endl;
    cout << sz(c) << endl;
}
